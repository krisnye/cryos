import { createAxis } from "graphics/database/index.js";
import { VoxelEditorStore } from "../voxel-editor-store.js";

export const createTestModels = (t: VoxelEditorStore) => {   
    createAxis(t);
};




