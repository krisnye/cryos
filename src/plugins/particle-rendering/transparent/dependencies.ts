// Shared dependencies for transparent particle rendering plugins
import { particleRenderingBaseDependencies } from "../dependencies.js";

// Transparent rendering uses the same base dependencies (which already includes transparent component)
export const particleRenderingTransparentDependencies = particleRenderingBaseDependencies;

